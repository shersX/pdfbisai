# 2026-09-16

## 比赛提分方案分析（多模态表格识别赛）
- 现状：77.0 分（qwen3.8-flash 单模型），排名 16/142，榜一 82.5。908 题 ≈ 每题 0.11 分。
- 题型分布：extract 652 / thinking 217 / structure 39；answer_format: string 396 / number 365 / json_array 107 / json 40。
- 关键发现：两次运行（qwen3-vl-plus vs qwen3.8-flash）答案一致 497 题、分歧 411 题（extract 235 / thinking 137 / structure 39 全部分歧）。分歧题集中了大部分错误 → 多模型投票+仲裁是最高性价比方案。
- 建议优先级：①分歧题第三模型仲裁投票 ②thinking 题让模型输出表达式、Python 求值 ③两段式（先转录整表为 HTML 再答题，同文件多题复用）④图像裁剪放大/PDF_ZOOM 提高 ⑤格式回归校验。
- 提交文件仅 1 题空答案，structure 题 JSON 全部合法且无几何越界（后处理已做好）。
- 环境：项目 .venv（python 3.13.14），入口 run_baseline.py，模型经 QWEN_VL_MODEL 环境变量配置，checkpoint 按 outputs/<模型名>/ 分目录。

## 方案①落地：三方投票合并（第 3 次提交）
- 用 qwen3.8-max-0902 仲裁重跑 411 道分歧题（410 完成，id=889 超时后已被投票兜底，答案来自旧 checkpoint 优先级回退）。
- 合并结果：全一致 497 / 多数投票 285 / 全分歧取 max 优先 126 / 空 0。校验通过（908 id 全、structure JSON 全合法无越界、0 空答案），已写入根目录 submission.xlsx 待提交。
- 代码改动：config.py 加 OUTPUT_SUBDIR（默认=模型名）；run_baseline.py 加 --ids-file；runner.py 部分重跑(<95%覆盖)不再覆盖根目录 submission.xlsx。
- 新工具：tools/make_diff_ids.py（JSON 规范化对比生成分歧 id）、tools/merge_vote.py（多 checkpoint 多数投票，--priority 指定全分歧兜底模型）。
- 下一步：提交看分；若需继续提分，做方案②（thinking 题表达式 + Python 求值）。qwen3.8-max 跑 411 题约 37 分钟（6 并发）。
